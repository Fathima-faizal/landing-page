<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Professional Admin Dashboard</title>
    <link rel="stylesheet" href="/css/addProduct.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/cropperjs/1.6.1/cropper.min.css">
</head>
<body>

    <div class="dashboard-container">
        <aside class="sidebar">
            <div class="nav-item ">
                <a href="/admin/dashboard">
                <i class="fas fa-th-large"></i> <span>Dashboard</span>
                </a>
            </div>
            <div class="nav-item active">
                <a href="/admin/product">
                <i class="fas fa-box"></i> <span>Product Management</span>
                </a>
            </div>
            <div class="nav-item">
                <a href="#">
                <i class="fas fa-shopping-cart"></i> <span>Order Management</span>
                </a>
            </div>
            <div class="nav-item">
                <a href="/admin/customer">
                <i class="fas fa-user"></i> <span>User Management</span>
                </a>
            </div>
            <div class="nav-item">
                <a href="#">
                <i class="fas fa-comment-alt"></i> <span>Review</span>
                </a>
            </div>
            <div class="nav-item">
                <a href="#">
                <i class="fas fa-truck"></i> <span>Order</span>
                </a>
            </div>
            
            <hr class="nav-divider">

            <div class="nav-item">
                <a href="#">
                <i class="fas fa-ticket-alt"></i> <span>Coupon</span>
                </a>
            </div>
            <div class="nav-item">
                <a href="#">
                <i class="fas fa-layer-group"></i> <span>Category Management</span>
                </a>
            </div>
            <div class="nav-item">
                <a href="#">
                <i class="fas fa-image"></i> <span>Banner Management</span>
                </a>
            </div>
            <div class="nav-item logout-btn">
                <a href="/admin/logout">
                <i class="fas fa-sign-out-alt"></i> <span>Logout</span>
                </a>
            </div>
        </aside>

        <main class="main-content">
            <header class="top-header">
                <h2>Product Management</h2>
                <a href="/admin/logout">
                <button class="logout-top">Log out</button>
                </a>
            </header>
            <section class="content-body">
                <h3>Add Product</h3>
       <form id="productForm" enctype="multipart/form-data">
    <div class="form-row">
        <div>
            <label>Product Name</label>
            <input type="text" name="productName" placeholder="Enter product name">
        </div>
        <div>
            <label>Category</label>
            <input type="text" name="category" placeholder="Enter category">
        </div>
    </div>

    <div class="form-row full-width">
        <div>
            <label>Description</label>
            <textarea name="description" placeholder="Enter product description"></textarea>
        </div>
    </div>

    <div class="form-row">
        <div>
            <label>Regular Price</label>
            <input type="number" name="regularPrice" placeholder="0.00">
        </div>
        <div>
            <label>Sale Price</label>
            <input type="number" name="price" placeholder="0.00">
        </div>
    </div>

    <div class="form-row">
        <div>
            <label>Stock Status</label>
            <input type="number" name="stock" placeholder="Enter stock count">
        </div>
        <div>
            <label>Quantity</label>
            <input type="number" name="quantity" placeholder="Enter quantity">
        </div>
    </div>

    <div class="image-upload-section">
        <h3>Product Images</h3>
        <div class="upload-group" style="border: 1px solid #ccc; padding: 15px;">
            <label>Product Image 1:</label>
            <input type="file" class="imageInput" data-index="0" accept="image/*">
            <div class="crop-wrapper" id="wrapper-0" style="display: flex; gap: 20px; margin-top: 10px;"></div>
        </div>

        <div class="upload-group" style="border: 1px solid #ccc; padding: 15px;">
            <label>Product Image 2:</label>
            <input type="file" class="imageInput" data-index="1" accept="image/*">
            <div class="crop-wrapper" id="wrapper-1" style="display: flex; gap: 20px; margin-top: 10px;"></div>
        </div>

        <div class="upload-group" style="border: 1px solid #ccc; padding: 15px;">
            <label>Product Image 3:</label>
            <input type="file" class="imageInput" data-index="2" accept="image/*">
            <div class="crop-wrapper" id="wrapper-2" style="display: flex; gap: 20px; margin-top: 10px;"></div>
        </div>
    </div>

    <div class="submit-container">
        <button type="submit" class="btn-save">Save Product</button>
    </div>
</form>
    </section>
<script src="https://cdnjs.cloudflare.com/ajax/libs/cropperjs/1.6.1/cropper.min.js"></script>
<script>
    const form = document.getElementById("productForm");
    const imageInputs = document.querySelectorAll(".imageInput");
    
    let finalBlobs = [null, null, null]; 
    let croppers = [null, null, null];

    imageInputs.forEach(input => {
        input.addEventListener("change", (e) => {
            const index = e.target.getAttribute("data-index");
            const wrapper = document.getElementById(`wrapper-${index}`);
            const file = e.target.files[0];

            if (!file) return;

            wrapper.innerHTML = "";
            if (croppers[index]) {
                croppers[index].destroy();
                croppers[index] = null;
            }

            const reader = new FileReader();
            reader.onload = (event) => {
                // 1. Create a container for the original image so we can hide it later
                const imgDiv = document.createElement("div");
                imgDiv.id = `img-container-${index}`; // ID for easy hiding
                imgDiv.style.width = "300px";
                
                const img = document.createElement("img");
                img.src = event.target.result;
                img.style.maxWidth = "100%";
                imgDiv.appendChild(img);

                const controlDiv = document.createElement("div");
                controlDiv.style.display = "flex";
                controlDiv.style.flexDirection = "column";
                controlDiv.style.gap = "10px";

                const saveBtn = document.createElement("button");
                saveBtn.innerText = "Confirm Crop";
                saveBtn.type = "button";
                saveBtn.style.cssText = "background: #007bff; color: white; padding: 5px 10px; border: none; cursor: pointer;";

                const previewImg = document.createElement("img");
                previewImg.style.cssText = "max-width: 150px; display: none; border: 2px solid #28a745; border-radius: 4px;";

                controlDiv.appendChild(saveBtn);
                controlDiv.appendChild(previewImg);

                wrapper.appendChild(imgDiv);
                wrapper.appendChild(controlDiv);

                croppers[index] = new Cropper(img, {
                    aspectRatio: 1,
                    viewMode: 1
                });

               saveBtn.addEventListener("click", () => {
    if (croppers[index]) {
        const canvas = croppers[index].getCroppedCanvas({ width: 440, height: 440 });
        
        // 1. Show the cropped result
        previewImg.src = canvas.toDataURL("image/jpeg");
        previewImg.style.display = "block";

        // 2. Destroy the cropper interface (removes the blue box and handles)
        croppers[index].destroy();
        croppers[index] = null;

        // 3. Keep the original image visible but make it look like a static preview
        // We do NOT use imgDiv.style.display = "none" anymore.
        imgDiv.style.border = "1px solid #ccc"; 
        imgDiv.style.opacity = "0.8"; // Optional: slightly fade it to show it's "finished"

        canvas.toBlob((blob) => {
            finalBlobs[index] = blob;
        }, "image/jpeg");

        saveBtn.innerText = "Saved ✅";
        saveBtn.style.background = "#28a745";
        saveBtn.disabled = true; 
    }
});
            };
            reader.readAsDataURL(file);
        });
    });

</script>
</body>
</html>