<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>VisionVogue - Wishlist</title>
    <link rel="stylesheet" href="/css/wishlist.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
     <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
</head>
<body>

    <header>
        <nav class="navbar">
            <ul class="nav-links">
                <li><a href="/home">Home</a></li>
                <li><a href="/shop">shop</a></li>
                <li><a href="#">About</a></li>
                <li><a href="#">Contact us</a></li>
            </ul>
            <div class="logo">VisionVogue</div>
            <div class="nav-icons">
               <a href="/wishlist" ><i class="fa-regular fa-heart"></i></a>
               <a href="/cart"> <i class="fa-solid fa-cart-shopping"></i></a>
              <a href="/profile" > <i class="fa-regular fa-user"></i></a>
            </div>
        </nav>
    </header>
     <div class="wishlist-container">
    <h2>Your Wishlist</h2>
    
    <table class="wishlist-table">
        <thead>
            <tr>
                <th>Image</th>
                <th>Product Details</th>
                <th>Price</th>
                <th>Action</th>
                <th>Remove</th>
            </tr>
        </thead>
        <tbody>
            <%if(wishlist.length>0){%>
                <%for(let product of wishlist){%>
            <tr>
                <td>
                    <img src="/uploads/<%=product.productimage[0]%>"  class="product-img">
                </td>
                <td class="details">
                    <span class="brand"><%=product.brand%></span>
                    <span class="product-name"><%=product.category.name%></span>
                    <span class="category"><%=product.productName%></span>
                </td>
                <td class="price"><%=product.salesPrice%></td>
                <td>
                  <button class="add-btn" onclick="addToCart('<%=product._id%>')">Add to Cart</button></a>
                </td>
                <td>
                    <button class="remove-btn">
    <a href="/wishlist/delete?id=<%=product._id%>" 
       onclick="return confirmDelete(event, '<%=product.category.name%>')" >
        <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="white" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
            <polyline points="3 6 5 6 21 6"></polyline>
            <path d="M19 6v14a2 2 0 0 1-2 2H7a2 2 0 0 1-2-2V6m3 0V4a2 2 0 0 1 2-2h4a2 2 0 0 1 2 2v2"></path>
        </svg>
    </a>
</button>
                </td>
            </tr>
            <%}%>
            <%}else{%>
                <tr>
                    <td colspan="5" style="text-align: center; padding: 40px 0;">
                        <p style="margin: 0; font-size: 18px; color: #666;">No items found in wishlist</p>
                    </td>
                </tr>
                <%}%>
            </tbody>
    </table>
    <div class="container">
                    <nav aria-label="page navigation">
                        <ul class="pagination justify-cotent-center mb-20"style="margin-right:20px";>
                            <%for(let i=1;i<=totalPages;i++){%>
                                <li class="page-item <%=(i===currentPage)? 'active':''%>">
                                 <a class="page-link" href="?page=<%= i %>"><%= i %></a>
                                </li>
                                <%}%>
                        </ul>
                    </nav>
                </div>
</div>
    <footer>
        <div class="footer-content">
            <div class="footer-column">
                <h4>About Us:</h4>
                <a href="#">Our Story</a>
                <a href="#">Privacy Policy</a>
                <a href="#">Terms</a>
                <a href="#">FAQ</a>
            </div>
            <div class="footer-column">
                <h4>Support:</h4>
                <a href="#">Shipping Policy</a>
                <a href="#">Returns</a>
                <a href="#">Order Tracking</a>
                <a href="#">Help</a>
            </div>
            <div class="footer-column">
                <h4>My Account:</h4>
                <a href="#">Profile</a>
                <a href="#">My Orders</a>
                <a href="#">Wishlist</a>
                <a href="#">Refer & Earn</a>
            </div>
            <div class="footer-column">
                <h4>Contact:</h4>
                <p>Email: visionvogue@gmail.com</p>
                <p>Phone: 8923457190</p>
            </div>
        </div>
        <p class="copyright">© 2026 Eyewear Store. All rights reserved.</p>
    </footer>
   <script>
    function moveSlide(index) {
    const wrapper = document.getElementById('offerWrapper');
    const dots = document.querySelectorAll('.dot');
    
    // Move the wrapper by 100% (which is 2 cards)
    wrapper.style.transform = `translateX(-${index * 50}%)`;
    
    // Update active dot
    dots.forEach(dot => dot.classList.remove('active'));
    dots[index].classList.add('active');
}
    window.addEventListener( "pageshow", function ( event ) {
        var historyTraversal = event.persisted || 
                               ( typeof window.performance != "undefined" && 
                                 window.performance.navigation.type === 2 );
        if ( historyTraversal ) {
            window.location.reload();
        }
    });
    window.history.pushState(null, null, window.location.href);
    window.onpopstate = function () {
        window.history.go(1);
    };
    function addToCart(productId) {
    $.ajax({
        url: '/cart',
        method: 'POST',
        data: { productId: productId },
        success: (response) => {
            if (response.status === true) {
                window.location.href = `/wishlist/delete?id=${productId}&redirect=cart`;
            } else {
                swal.fire({ icon: 'error', title: 'Error', text: response.message || 'Already  add to cart' });
            }
        }
    });
}
    function confirmDelete(event, name) {
    if (!confirm(`Are you sure you want to delete "${name}"?`)) {
        event.preventDefault(); 
        return false;
    }
    return true;
}
</script>
</body>
</html>